<% if notice %>
  <div class="alert alert-success" role="alert">
    <%= notice %>
  </div>
<% end %>

<%= render @cart_product %>

<div class="mt-3 d-flex gap-2">
  <%= link_to "Modifier ce produit", edit_cart_cart_product_path(@cart_product), class: "btn btn-primary" %>
  <%= link_to "Retour au panier", cart_path, class: "btn btn-secondary" %>
  <%= button_to "Supprimer ce produit", cart_cart_product_path(@cart_product), method: :delete, data: { confirm: "Es-tu sûr(e) de vouloir supprimer ce produit du panier ?" }, class: "btn btn-danger" %>
  

</div>
